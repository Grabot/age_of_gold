
double xSize = 17;
double ySize = 8;
int radius = 4;

double maxSpeed = 80;

// We define an offset, this would be when you should retrieve new hexagons
int tileOffset = radius * 2 + 1;

int mapSize = 500;
